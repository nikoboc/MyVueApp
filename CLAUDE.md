# CLAUDE.md

**TimeCard** のプロジェクト規約である。Vue の学習用に構築する勤怠打刻アプリを対象とする。出勤・退勤・
休憩開始・休憩終了を記録し、過去の記録を修正できる。設計の全体は [`docs/`](./docs/README.md) に記載
している。ストアまたは集計処理を変更する前に `docs/03-state-and-data.md` を参照すること。

## AI アシスタントへ（必須）

本ファイルおよび [`docs/05-typescript-conventions.md`](./docs/05-typescript-conventions.md) の規則は、
参考ではなく必須である。コードを記述または変更する際は必ず従うこと。

コミットを提案する前、および実際にコミットする前に、変更したファイルを本規約および下記の
アーキテクチャ規約と照合し、違反があれば先に修正すること。規約に違反したコードをコミットしては
ならない。自己レビューの際は、確認した規則を簡潔に述べること。

## 前提

- **オーナーは Java のバックエンドエンジニア（7 年）であり、Vue を学習中である。** 説明は Java、
  オブジェクト指向、バックエンドの概念から接続する形とする。Vue の新しい概念を導入する際は、名称と
  対応する概念を併せて示すこと。
- **学習用のプロジェクト**であるため、技巧的なコードよりも、明快で定石に沿った説明しやすいコードを
  優先する。「何をしているか」に加えて「なぜそうするか」も説明すること。

## 技術スタック（変更しない）

- **Vue 3** — Composition API と `<script setup>` のみを使用する。Options API は使用しない。
- **TypeScript** — strict。ドメインの境界とストアの状態には型を明示する。
  [`docs/05-typescript-conventions.md`](./docs/05-typescript-conventions.md) に従うこと。
- **Pinia** — setup 形式のストア（`defineStore("x", () => { ... })`）。
- **vue-router** — 画面遷移。**ハッシュモード（`createWebHashHistory`）を用いる。**
  履歴モードでは `/MyVueApp/monthly` のようなパスがサーバーへ届き、GitHub Pages には
  その名前のファイルが無いため 404 になる。ハッシュより後ろはサーバーに送られないため、
  配信側の設定が不要で、オフラインでも同じように動作する。
- **Vite** — ビルドおよび開発用のツール。
- **`localStorage`** — 保存先。サーバー、API、データベースは使用しない。

## アーキテクチャ規約（変更不可。これを判定基準とする）

1. **状態はストアに集約する。** グローバルな状態は Pinia に配置する。コンポーネントはストアから参照し、
   アクションを起動する。一時的な UI 状態を超える状態をコンポーネントが保持してはならない。
2. **props down, events up。** コンポーネントはデータを `props` で受け取り、上位へは `emit` で伝達する。
   ただし打刻を管理する `PunchPanel` と `DayCard` は、自身が主体である操作に限りストアのアクションを
   直接呼んでよい。末端の `PunchRow` と `PunchForm` は状態を変更せず、意図を emit するにとどめる。
3. **コンポーネントから保存層を直接呼ばない。** 呼ぶのはストアのアクションであり、`punchStorage.ts` を
   呼ぶのはストアである。
4. **サービス層に Vue を含めない。** `src/services/` と `src/types/` は Vue を一切 import しない純粋な
   TypeScript とし、単体テスト可能かつフレームワークに依存しない状態を保つ。
5. **導出値を保存しない。** 保存するのは打刻の列だけであり、実働時間・休憩時間・勤務状態はすべて
   `services/attendance.ts` で導出する。集計を状態として持つと、打刻を修正したときに両者が食い違う。
6. **集計処理は現在時刻を参照しない。** `summarizeDay` は入力だけで結果が決まる純粋な関数とする。
   進行中の区間の経過時間は、`useNow` を持つコンポーネント側で算出する。
7. **副作用はクリーンアップする。** `onMounted` で開始した `setInterval` やリスナーは、必ず
   `onUnmounted` で破棄する（コンポーザブルへの切り出しが望ましい）。
8. **外部から読み込んだデータは検証する。** `localStorage` の内容は `unknown` として受け、型ガードで
   絞り込んでから使用する。URL のパラメーターも同様に、利用者が書き換えられる入力として扱う
   （`MonthlyView` の月キーが該当する）。

## 命名と構成

- コンポーネント: `PascalCase.vue`。再利用可能なものには `Base` を付与する。
- 画面: `src/views/` に `XView.vue`。ルートに対応する単位であり、`src/components/` の
  部品とは役割が異なるため、ディレクトリを分ける。
- ストア: `useXStore.ts`。`useXStore` として export する。
- コンポーザブル: `src/composables/` に `useX.ts`。
- 識別子の命名（変数、関数、定数、真偽値、ジェネリクス）は
  [`docs/05-typescript-conventions.md`](./docs/05-typescript-conventions.md) §16 に従う。
  ミリ秒を保持する値には `Ms` を付ける（`workedMs`、`intervalMs`）。
- ディレクトリ構成は `docs/02-architecture.md` に従う。

## 表示言語

- **UI に表示される文字列、コード内のコメント、ドキュメントはすべて日本語で記述する。** ボタン、
  ラベル、プレースホルダー、`aria-label`、エラーメッセージを含む。
- 文体は**常体（だ・である調）で統一する。** ただし UI 上で利用者に向ける文言は丁寧語でよい。
- 日本語は**英文の直訳としない。** 以下に注意すること。
  - 読者への呼びかけや推量（「〜してほしい」「〜だろう」「〜はずだ」「覚えておきたい」）を用いず、
    事実を淡々と記述する。
  - 英語の比喩をそのまま訳さない。意味の通る日本語の表現に置き換える。
  - 英文の構文をなぞらない。1 文が長くなる場合は分割する。
- **コードの識別子は英語のままとする**（変数名、関数名、型名、ファイル名、Pinia のストア ID、
  emit するイベント名）。§16 の命名規約をそのまま適用する。

## ドメインの用語

| 日本語 | 識別子 | 意味 |
|------|--------|------|
| 打刻 | `Punch` | 記録の最小単位。これだけを保存する |
| 出勤／休憩開始／休憩終了／退勤 | `clock-in` / `break-start` / `break-end` / `clock-out` | 打刻の種別 |
| 勤務状態 | `WorkStatus` | 未出勤・勤務中・休憩中・退勤済み |
| 在社時間 | `presentMs` | 出勤から退勤までの合計 |
| 休憩時間 | `breakMs` | 休憩開始から休憩終了までの合計 |
| 実働時間 | `workedMs` | 在社時間から休憩時間を引いたもの |
| 不整合 | `PunchIssue` | 対にならない打刻（打刻漏れなど） |

## 進め方

- **`docs/04-roadmap.md` のフェーズに従って実装する。** 1 単位につき 1 つの概念とし、順序を飛ばさない。
- **フェーズごとにコミットする。** Conventional Commits 形式（`feat:`、`docs:`、`chore:`、`refactor:`）。
  コミットは意図的に小さく、読みやすく保つ。
- コミットおよびプッシュは、明示的に依頼された場合にのみ行う。

## コマンド

- `npm install` — 依存関係をインストールする
- `npm run dev` — Vite の開発サーバーを起動する（http://localhost:5173）
- `npm run dev:host` — LAN からも接続できる形で開発サーバーを起動する（実機確認用）
- `npm test` — テストを 1 回実行する
- `npm run test:watch` — 変更を監視してテストを実行する
- `npm run build` — 型チェック（`vue-tsc --noEmit`）と本番ビルド
- `npm run type-check` — 型チェックのみ
- `npm run preview` — 本番ビルドをプレビューする

## テスト

- テストは Vitest で書き、**実装と同じ場所に `*.spec.ts`** として置く。どこにテストがあるか
  探す必要がないようにするためである。
- `describe`/`it`/`expect` は `vitest` から明示的に import する。グローバルに頼らない。
- **サービス層を優先して書く。** `services/` は Vue を含まない純粋な関数であり、モックを用意せず
  引数と戻り値だけで検証できる。ここが厚ければ、コンポーネントのテストは薄くて済む。
- ストアのテストでは `setActivePinia(createPinia())` で本物のストアを使う。`createTestingPinia` は
  アクションを差し替えてコンポーネントを隔離するためのもので、アクション自体の検証には使わない。
- **仕様として固定したい判断はテストに書く。** 「日をまたぐ勤務は表せない」「休憩の重なりを拒否する」
  「中抜けの日は変換で実働が変わる」などは、コメントではなくテストで残す。
- 動作を変えたら、対応するテストが落ちることを確認する。通るだけで壊れを検出できないテストは
  意味がない。
- `npm test` は CI でビルドより先に実行される。失敗すれば公開されない。

> ⚠️ jsdom は `<dialog>` の `showModal()` / `close()` を実装していない。`vitest.setup.ts` で補って
> いるため、ダイアログを含むコンポーネントもテストできる。

## 公開と実機での確認

### 公開（GitHub Pages）

`main` へ push すると `.github/workflows/deploy.yml` が型チェック・ビルド・公開まで自動で行う。
公開先は `https://<ユーザー名>.github.io/<リポジトリ名>/` である。

base パスはリポジトリ名のサブパスになるため、`vite.config.ts` が `GITHUB_REPOSITORY` から導出する。
リポジトリ名をコードに書かないのは、名前を変更したときの修正漏れを避けるためである。

### 開発中の実機確認

- **同じ Wi-Fi から:** `npm run dev:host` を実行し、表示される Network の URL
  （`http://<PC の IP>:5173`）を端末で開く。
- **一時的に外部から:** `cloudflared tunnel --url http://localhost:5173` を実行し、発行される
  `*.trycloudflare.com` の URL を開く。`vite.config.ts` の `allowedHosts` で許可済みである。

### オフライン対応（PWA）

`public/sw.js` が画面を構成するファイルを先読みし、通信できない場所でも起動できるようにしている。
打刻の保存先が端末内であるため、オフラインでも記録・修正がそのまま行える。

先読みするファイルの一覧は、ビルド後に `scripts/generate-sw-precache.mjs` が `dist` を走査して
埋め込む。JS と CSS のファイル名にはビルドごとに変わるハッシュが含まれ、`public/sw.js` に直接
書けないためである。**Service Worker を変更したら、ビルドしてキャッシュの動作を必ず確認すること。**

> ⚠️ キャッシュの照合では `ignoreVary: true` を指定している。多くの配信元が `Vary: Origin` や
> `Vary: Accept-Encoding` を返す一方、先読み時と実際の読み込み時ではリクエストヘッダーが異なり、
> 既定の照合では一致しない。この指定を外すとオフラインで起動できなくなる。理由は `public/sw.js` の
> コメントに記載している。

### 記録の保存範囲

保存先が `localStorage` であるため、**記録は開いた端末とオリジンごとに独立する。** PC で打刻した
記録はスマートフォンには現れず、`localhost` と公開 URL でも別の記録として扱われる。端末をまたいで
共有するには保存先をサーバーへ移す必要があり、現時点では対象外である（docs/01 の対象外を参照）。

## ツールチェーンの制約（これより新しいバージョンへ更新しない）

- **TypeScript は 5.x に固定する**（`^5`）。TypeScript 7（ネイティブコンパイラ）は公開されているが、
  `vue-tsc` 3 が未対応である。`typescript@latest` を導入するとビルドが失敗する（`./lib/tsc` が
  export されていない）。`vue-tsc` が TypeScript 7 対応を告知するまでは 5.x を維持する。
- **Pinia は 3.x に固定する**（`^3`）。Pinia `4.0.1` は `exports` マップに `types` 条件が欠けた状態で
  公開されており、TypeScript が型定義を解決できない。この不具合が修正されるまで 4.x は使用しない。
